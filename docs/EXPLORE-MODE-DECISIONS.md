# Explore mode — decision log

Every judgement call made during the Explore mode build, written down as it
was made.

**Append to this file the moment a decision is made.** Not at the end of a
sprint, not at the end of the run. A run this long loses context, and a log
reconstructed from memory afterwards is a fiction — it will be confidently
wrong about the reasoning, which is the only part worth keeping.

The last column is the one that matters. It is what lets the owner overturn a
decision without having to reverse-engineer it first: what it would cost, and
what he would get instead. A row without a real answer there is not finished.

Rules for this file:

| | |
|---|---|
| One row per decision | Not per commit, not per sprint |
| Small decisions belong here too | The point is a complete record, not a highlights reel |
| Escalated decisions belong here | With what the owner said, so the reasoning survives |
| Never edit a row after the fact | Add a new row that supersedes it, and say so |

---

## Decisions

| # | Sprint | Decision | Why | What changes if the owner disagrees |
|---|---|---|---|---|
| 1 | 0 | Work on `claude/spaces-lab`, as instructed, not on the session's default branch `claude/spaces-lab-lk3n7u` | The owner named the branch explicitly and it is the one Vercel builds a preview from. Both branches pointed at the same commit when the run started, so nothing was lost | Nothing. `git push -u origin claude/spaces-lab-lk3n7u` from the same commits, one command |
| 2 | 0 | Add a development fixture dataset — `fixtures/dev-buildings.json`, served by `/api/buildings` **only** when `SPACES_FIXTURE_DB=1` | This container has no `DATABASE_URL` and no way to get one. Without a fixture the map is empty, no screenshot exists, and none of the seven existing browser harnesses can run — which would make every visual kill criterion in the plan unverifiable | Delete `src/lib/dev-fixture.ts`, the four-line guard in `/api/buildings`, `fixtures/` and `scripts/make-dev-fixture.ts`: ~10 minutes. Cost of doing so is that this branch can no longer be verified anywhere without a database |
| 3 | 0 | Geometry, BINs, heights, floor counts and years in the fixture are real NYC Open Data. Availability and tenancy rows are invented, and every one carries `FIXTURE — synthetic development data` in its own notes | The plan forbids inventing *product* data. Development scaffolding that never reaches production and is labelled in every row is a different thing, and the alternative was no visual verification at all for the whole run | Point the fixture generator at a read-only copy of the real database instead: ~1 hour, and needs a connection string this container does not have |
| 4 | 0 | The fixture is a fallback for the **flag**, never for a database failure. A missing or broken `DATABASE_URL` still errors exactly as it does today | A broker must never be shown invented availability because a query timed out. Silent fallback is how that happens | None available — this one is not worth overturning |
| 5 | 0 | Baseline recorded as 100 passing / 7 failing browser checks before any Explore code was written. The 7 are `verify-add-by-hand` ×5 (needs database writes) and `verify-map-chrome` ×2 (needs a second tower to land under a projected screen point) | A regression detector needs a known-good starting count. Reporting "7 failures" at the end without this baseline would look like damage caused by this work | Nothing to change; this is a measurement, not a choice |
| 6 | 0 | `three` 0.185.1 (MIT) as the only new runtime dependency | The plan settles three.js. MIT is permissive; the module graph actually imported is roughly 170 KB gzipped, well inside the 5 MB load budget | Nothing else in the plan works without it |
| 7 | 1 | The three.js scene is **metres**, `+X` east `+Y` north `+Z` up, bridged to Mercator by one translate-and-scale in the layer | A mullion is 4e-9 Mercator units, which is past a 32-bit float's resolution. Every shader that wants a distance in metres would otherwise have to undo the projection first | Nothing to change — this is the standard bridge. Moving the anchor is what costs: it is fixed for the layer's life because it is the origin every vertex is relative to |
| 8 | 1 | Camera position derived from MapLibre's **public** accessors (`getCenter`/`getZoom`/`getPitch`/`getBearing`) using its own camera model, not read off `map.transform` | `transform` is not in the published type surface and has changed shape between versions. This project has already been bitten once by building on an internal | Read `map.transform.getCameraPosition()` instead: 5 lines, and it re-couples us to a private field |
| 9 | 1 | three.js **colour management off** for Explore mode | Since r152 `new Color('#8FB4E4')` is converted sRGB→linear while `new Color(0.9,0.9,0.9)` is not. Explore mixes both — sky from the atmosphere presets as hex, stone as numbers — so half the palette was silently a stop and a half dark. The city rendered slate blue and every individual number was correct | Turn management on and convert the numeric colours instead: ~1 hour, same result, more places to get it wrong |
| 10 | 1 | Explore renders **display-referred**: no tone mapping, no sRGB encode | This renderer writes into MapLibre's own drawing buffer and composites under deck.gl, and both write display values with no encode step. Explore has to live in their colour space, not its own | Adding a tone-mapped pass means re-grading deck.gl's bands to match: half a day, and the two modes would then differ in how loud Goldenrod looks |
| 11 | 1 | In Explore mode deck.gl draws **only what belongs on top of the world** — bands (invisible, for picking), name-plates, transit, radius. The ground, the massing, the roofs and the floor lines all move to three.js | deck.gl's overlay is a separate canvas composited above MapLibre's, so anything opaque it draws paints over the three.js city entirely. This is not a preference, it is what the compositing allows | Switch the deck.gl overlay to `interleaved: true` so both share one buffer: ~1 day, and it changes the flat map's render path, which is the thing that must not change |
| 12 | 1 | The buildings layer and the bands layer stay in deck.gl in Explore mode, drawn **fully transparent**, purely to keep them clickable | Picking runs in its own framebuffer and is indifferent to alpha, so every popup, fly-to, hover and Compare behaviour carries over with no changes in either mode. Rebuilding picking as a three.js raycast would be a second implementation of a solved problem | Raycast in three.js and delete the invisible layers: ~1 day, plus re-testing every click path the flat map already proves |
| 13 | 1 | The **visible** availability bands are drawn in three.js, from the same `computeBands()` the flat map calls | With them on deck.gl's canvas every band showed all four sides at once and read as a yellow wireframe box floating beside the tower rather than a stripe on it. That is availability getting *less* legible, which fails the one rule. Same maths means the two can never disagree about a floor | Move them back and accept the floating look: one line. Not recommended — this was the single biggest legibility win in sprint 1 |
| 14 | 1 | A band's collar is taken from the **massing's width at that height**, not the ground footprint (`lib/explore/profile.ts`) | This is §5's argument as code. On a stepped tower a collar drawn on the plot hangs in mid-air above the first setback. The flat map is unaffected: it draws a plain prism, where the footprint is the answer at every height | Drop `profile.ts` and pass the footprint through: 10 minutes, and every band above a setback floats again |
| 15 | 1 | Explore mode's city is **colourless** — colour-by-rent applies to the flat map's massing only | The art direction is explicit that availability, client space and the selection are the only saturated things. A city tinted by rent would be competing with the bands everywhere at once | Tint the stone by rent mode: ~2 hours. It directly weakens the one rule, so it should be a deliberate choice rather than a default |
| 16 | 1 | Glass reflectance is 34% face-on rather than the textbook 4% | A curtain wall is mostly glass, so whatever the glass does the building does. At 4% with a tinted pane the city came out dark slate — striking, and the exact opposite of the brief. Coated architectural glazing really is near a third | Lower it and the city darkens; a dark city makes every lit window compete with the bands |
| 17 | 1 | The harness judges "availability leads" on **chroma**, not brightness | A near-white city in sunlight is legitimately brighter than `#FFB600`. Asserting on brightness would force a grey city and contradict the art direction. What makes a band lead is being the only saturated thing in the frame | Assert on brightness instead and the city has to go grey: the measurement change is 5 minutes, the art direction change is the whole look |
| 18 | 1 | Every setback step gets its own lid, not just the topmost | Capping only the top left a hole at each setback you could see the sky through. On screen it read as the tower coming apart rather than as a missing face, which is why only a screenshot found it | None — this was a bug, not a choice |
| 19 | 2 | `ExploreLayer.projectToScreen` and `.budget` are exposed, and the layer is put on `window.__explore` | Sprint 2's kill criterion is that bands and facades must not disagree about a floor, and that can only be proved by projecting a floor to a pixel and looking at it. A unit test would check the same arithmetic twice | Delete both and the alignment check becomes an eyeball: 10 minutes to remove, and the project loses its only mechanical guard on the thing §5 is about |
| 20 | 2 | The alignment probe is a **storey-tall strip across the near facade**, not a point | A point at the centroid lands a hundred pixels off the band on the near wall; a point at a footprint corner misses a band that has stepped inward above a setback. Both are true facts about geometry and neither is the question, which is purely vertical | None — three cheaper probes were each wrong in a different way |
| 21 | 2 | Projection rejects points with `w <= 0` | A point behind the camera divides to a perfectly plausible on-screen pixel. The harness probed it, found no band, and reported floor 63 broken when it was simply above the top of the frame. `Vector3.applyMatrix4` does that divide silently | None — this was a real bug in the projection helper |
| 22 | 2 | The context city is **one merged mesh**, not one per building | At forty thousand footprints the draw-call budget binds long before the triangle budget: forty thousand meshes is forty thousand state changes a frame against a budget of 1,000. Measured: the whole visible city is 151,179 triangles in **one** call | Split it for per-building culling: ~half a day, and it buys nothing until a single viewport holds more than about 2M triangles, which it does not |
| 23 | 2 | Budget measured, not asserted: **151,179 triangles and 19 draw calls** for the whole visible city with Explore on | §9 asks for it to be validated in sprint 2. It is 7.6% of the triangle budget and 1.9% of the draw-call budget, so the room for glass, interiors and street life is real rather than hoped for | Nothing — this is a measurement |
| 24 | 2 | Frame time is reported as "not pathological", with the reason, rather than as a 60 fps claim | The harness runs on SwiftShader in a container, one to two orders of magnitude slower than a broker's laptop. A 60 fps assertion here would either fail always or be meaningless | Run the harness on a machine with a GPU and tighten the threshold: a config change, but it needs hardware this container does not have |
| 25 | 3 | `scripts/fetch-lod2-massing.ts` gains `--from=<json>`, so the asset can be rebuilt without a database | The BIN list was the only thing the script needed Neon for. A development container, or anyone reproducing the asset from the checked-in fixture, could not run it at all | None — the database path is unchanged and still the default |
| 26 | 3 | A band's ring is a **real cross-section** of the surveyed massing, not a scaled footprint | Three weaker versions were each visibly wrong. A scalar shrink applied about the plot's centroid cannot describe a shaft that is off-centre in its plot or a different aspect ratio from it, and the Empire State Building is both — its bands ended up outside the tower on the short axis and inside it on the long one simultaneously | Revert to `insetForBuilding`: 20 minutes, and every band on a tower with an off-centre shaft goes back to floating |
| 27 | 3 | The cross-section is the **convex hull** of the slice, not a stitched loop | Ordering the segments into a loop needs adjacency the model does not publish, and getting it wrong produces a self-intersecting ring — a bow tie drawn across the building. A hull is always simple, always the right size and place, and a collar wraps the outside of a building anyway | Stitch properly: ~1 day plus a robustness problem. It buys one thing — a courtyard building's band would stop crossing its light well |
| 28 | 3 | Sections are cached per building per metre of height | Band geometry rebuilds on every filter keystroke, selection and colour change, and a slice walks every wall polygon — 469 of them on the Empire State Building | Drop the cache: one line, and filtering becomes visibly slow on a laptop |
| 29 | 3 | Surveyed reach is reported in **metres**, not as a fraction of reach at ground | The footprint dataset and the massing model do not agree about a building's extent at ground level — different surveys, different ideas of where a plinth ends. A ratio between two different measurements compounds the disagreement, and put ESB's bands a fifth of the way inside its own shaft | None — this was a units bug |
| 30 | 3 | The verification probe reads the **massing's** wall position, never the band's | Otherwise the test asks the code where it drew and then agrees with it. Massing and bands are separate code paths — surveyed surfaces or an extrusion, against `computeBands` | None available that is still a test |
| 31 | 4 | A bay is pier, mullion, transom, spandrel and pane — five parts, not two | A single sheet of glass a storey tall and a bay wide exists on very few buildings, and without the divisions a glass tower reads as shrink-wrapped. That is the specific way a procedural facade looks fake | Drop the transom and centre mullion: 20 lines. It costs the close-up view, which is the one this mode exists for |
| 32 | 4 | The mullion is a fixed 90 mm, not a fraction of the bay | Curtain wall is made of extrusions that come in one size. A proportional mullion makes a narrow bay's frame narrow, which no real building does | None worth having |
| 33 | 4 | Lit windows at dusk and night only, off at midday, deterministic per building from its id | A warm speckle across every glass tower in daylight is invisible as an effect and visible as noise, and Goldenrod is supposed to be the only warm thing in the frame. Seeding from the id means the same windows are lit every time the page opens | Light them at all hours: one line. It puts warm colour into the frame the hierarchy has no room for |
| 34 | 4 | The ground plane is lit by the **hour**, not by the light/dark theme | Keyed off the theme it left a near-white pavement under a black sky with a dark city standing on it, which reads as a mistake because it is one | None — this was a bug |
| 35 | 4 | Facade detail is verified against a **control in the same frame** — a patch of bare ground plane — not against an absolute threshold | The first absolute threshold was wrong by a factor of three, in the direction that fails a working feature. The honest question is not "is contrast above N" but "does this surface have structure that a surface without structure does not". Measured: 0.0061 on the wall against 0.0000 on bare ground | Pick a number instead: 2 minutes, and it will be wrong again the next time the lighting changes |
| 36 | 4 | No backticks anywhere in this file's GLSL | The shaders live in template literals, and a backtick in a GLSL comment ends the literal — producing a TypeScript syntax error a hundred lines away from the cause | None — this is a note to the next person |
| 37 | 5 | `lib/roofscape.ts` is reused unchanged; only its *rendering* is promoted | It already derives parapets, tiers, plant, tanks and masts from footprint, height, floor count and year built, deterministically and with no hand authoring. Sprint 5 is a rendering sprint, not a rederivation | None — rewriting it would throw away a tested generator |
| 38 | 5 | On a **surveyed** building the roofscape is derived against the massing's own crown — the largest roof surface and its cross-section — not the plot | A derived roofscape assumes the roof is at `height_roof_ft` with the footprint's outline. On a stepped tower neither is true, and the furniture would float beside the building. Same class of error as a band on the plot | Feed it the footprint: one line, and every water tank on a surveyed tower ends up in mid-air |
| 39 | 5 | The crown is picked by **area**, not by height | On a real tower the topmost surface is a few square metres of antenna cap. Putting the building's plant room on it is absurd in a way that is very easy to ship | Pick by height: one line, and the Empire State Building gets a plant room on its mast |
| 40 | 5 | A parapet is a hollow wall with a coping, not a solid slab | A solid parapet seen from above is a plate covering the roof, which hides the tanks and plant standing on it — the whole reason the roof is worth drawing | Extrude a solid ring: simpler code, and the roofscape disappears from every view above it |
| 41 | 5 | Roof furniture is merged into its building's own mesh | A tower's roof is a dozen small pieces; at 400 buildings that is five thousand draw calls against a budget of one thousand. It shares the building's material anyway — same stone, never coloured by data | Separate meshes for per-piece culling: half a day, and it buys nothing at this scale |
| 42 | 5 | Water tanks are ten-sided, not thirty-two | A tank is a few pixels tall from anywhere this map is read. Its silhouette is the point; smoothness is not | Raise it: one number, and the triangle count of every pre-war roof triples for no visible gain |
| 43 | 6 | The walk drives **MapLibre's own camera** rather than a camera of its own | MapLibre's camera is what deck.gl's overlay and the three.js scene both derive their projections from. One `jumpTo` moves the basemap, the bands, the name-plates, the transit lines and the city together; a separate walk camera means keeping three in step every frame, and the first frame they drift a band parts company with the window it is on | Add a walk camera and sync three projections: ~2 days, and it re-opens the alignment problem sprint 2 closed |
| 44 | 6 | The walker collides with the **footprint**, ours and the context city alike | At eye height a building is its ground floor, and that is what a footprint is. Walking through the anonymous grey building on the corner but not through the one we hold a listing for is worse than no collision — it teaches you not to trust the walls | Collide only with our own buildings: one line, and 40,000 buildings become fog |
| 45 | 6 | Collision resolution runs three passes | On a Manhattan corner two footprints share an edge, and one pass resolves the first while leaving the walker inside the second | Single pass: faster by nothing measurable, and corners stop working |
| 46 | 6 | "Which way is out" is **tested**, not derived from the ring's winding | A walker landing exactly on a wall is called inside by `pointInRing` and has no direction to `nearestEdge`; the fallback normal's sign depends on a winding NYC's data does not guarantee. The push went the wrong way and drove the walker into the building one step at a time until they stood in the middle of it | Normalise winding everywhere instead: it is already done for obstacles, and this is the belt to that's braces. Removing it re-opens a bug that needed 120 marching directions to find |
| 47 | 6 | Pitch clamps at 88°, not 85° | The map's centre is solved from the camera's own view direction, and at exactly 90° that solve divides by zero. MapLibre stops at 85 for the same reason; Explore mode exists to go past that | Lower it to 85: one number, and looking up a facade from the pavement stops being possible, which is the view this mode exists for |
| 48 | 6 | The time step is clamped to 100 ms | A backgrounded tab hands back a delta of several seconds, and an unclamped step puts the walker through the far wall of the block | None — this is a bug guard |
| 49 | 6 | The context-city harness check **polls** rather than sleeping | A fixed nine-second wait passed for three sprints and then failed for reasons unconnected to the code, because the fetch time depends on the cache and the viewport. The natural response to that is to assume the feature broke | None worth having |
| 50 | 7 | Interior mapping is a five-face parallax raycast in tangent space, not a texture | It is what makes glass worth having: a mirror is a surface, a room is a place. The room shifts correctly as you walk past — back wall slow, side walls sweeping — which is the entire cue that says depth. Costs a handful of instructions on fragments already being rasterised: no geometry, no texture, no draw call | Replace with a flat tint: 30 lines removed, and the plan's highest-payoff item goes with it |
| 51 | 7 | The room is a lit box with a ceiling glow and a desk line, and nothing else | Modelling interiors per space by hand is a stated non-goal, and a floor plan invented for a space we hold no drawing of would be the map telling a broker something it does not know | Add furniture: any amount of work, and each piece is a claim about a space we have not seen |
| 52 | 7 | Interiors are off entirely in daylight | A warm speckle across every glass tower at noon is invisible as an effect and visible as noise. Goldenrod is meant to be the only warm thing in the frame | One line to turn on, and it spends legibility for nothing |
| 53 | 7 | The floor plate shares its maths with the band: elevation from `floorHeightFt`, outline from the same cross-section `bandRingAt` uses | If the two ever parted company a broker would step onto the 14th floor and find the Goldenrod band at their ankles. The browser check compares the eye's height against the elevation derived from the **sheet**, not against the plate | Compute it separately: it would work, until the day one of them changed |
| 54 | 7 | Inside a building, collision **inverts** — you may go anywhere on the plate and nowhere beyond it | Walking into the glass stops you at the glass, which is correct and slightly uncanny: you are on the 14th floor | None sensible |
| 55 | 7 | A floor is entered from a space's own card, never by walking into a door | There are no doors, no lobbies and no cores in this model, and inventing one would be a lie about the building. The card is where a broker already is when they care about that floor | Add a door: it needs an interior model this mode explicitly does not have |
| 56 | 7 | Only the building you are standing in is drawn two-sided | A closed shell never needs its inside drawn, and drawing it doubles the fragment cost of the entire city. That trade is worth reversing for exactly one building | Make everything two-sided: one line, and the whole city pays for one room |
| 57 | 7 | Escape from a floor puts you on the street, not straight back to the drone camera | One step back at a time is what the key means everywhere else in this app | Swap the order: two lines |
| 58 | 8 | Cars and people are **neutral grey**, no colour at all | A street full of red and blue cars is the fastest way to lose the one rule: they are small, they move, and moving saturated colour beats static saturated colour every time | Colour them: one line, and availability stops leading in every frame with a street in it |
| 59 | 8 | Agent distribution carries the remainder between roads instead of rounding each independently | With 420 agents over a couple of hundred kilometres of centreline, almost every segment is owed a fraction of one. Rounding each to zero gave Midtown two cars | None — this was a bug |
| 60 | 8 | The roadbed and pavements are drawn in three.js, from the same centrelines the flat map uses | Until this existed the walk put you on a featureless grey plane with cars sliding across it, which reads as a bug rather than as traffic | Remove them and the street level has no street |
| 61 | 8 | Roadbed and pavement are 40% apart in value, not 4% | The first pair were four percent apart and the street did not read at all. The art direction wants clean pale streets with no grime; it is satisfied by near-white surfaces that differ, not by surfaces so close the kerb disappears | Narrow it and the street disappears again |
| 62 | 8 | The scene contains **no `THREE.Light` at all**; everything is shaded explicitly from the atmosphere preset | The traffic's stock Lambert material rendered every car and person black. This camera has its projection matrix assigned directly with an identity world matrix, which three.js's lighting is not built for. Shading by hand is also what keeps the two modes agreeing about how bright a band looks | Debug a light rig for two materials: unknown hours, and it re-introduces a second lighting model beside the one the shaders use |
| 63 | 8 | Frame cost is checked as a **ratio against the same scene without traffic**, not against a millisecond budget | Any absolute number picked on SwiftShader would either fail always or prove nothing. A ratio carries over to hardware | None — an absolute number here is not a measurement |
| 64 | 8 | The surrounding-city harness check asserts on the **context** triangle count, not the total | Once the streets landed, the total cleared the threshold whether or not the surrounding city had arrived. It was a weaker check than it looked | None — this was a weak assertion, found and tightened |
| 65 | 9 | **Sprint 9 trimmed.** The flat map does not get cars, people or the surveyed skyline | deck.gl's overlay is a separate canvas composited above MapLibre's, so three.js traffic underneath it is hidden by the flat map's own ground. Delivering it means either a new deck.gl mesh-layer dependency plus a per-frame loop in the live map's render path, or switching the overlay to `interleaved: true` so both share one buffer. The second changes the render path of a tool brokers use in front of tenants today, in a codebase whose brief records deck.gl's shader and picking passes breaking building clicks twice | **~3 days and a real risk budget.** Interleaved mode is the clean answer and it needs the seven existing browser suites re-run against every layer, plus the picking guards in `mullions.ts` and `atmosphere.ts` re-proved. The owner's call, not mine — it is exactly the "change brokers would see today" the plan reserves |
| 66 | 9 | What ships instead is a **leak guard**: a test that loads the surveyed massing and asserts the flat map's geometry does not move | Explore added a second source of truth about what a building looks like, and `computeBands`, `computeFloorLines` and `buildingRing` are shared. The risk was never that the massing is wrong — it is that it leaks and the flat map changes under a broker with no warning | Delete it and lose the only mechanical guarantee that the two modes stay separate |

| 67 | 10 | One evidence frame per sprint is committed to `docs/evidence/`; the rest of `shots/` stays gitignored | The kill criteria are visual, so the closing report has to point at the frames the work was judged on rather than at a rebuild that might not reproduce them. Sixty-five megabytes of harness output is not that; 3.4 MB of chosen frames is | Delete them: the report's evidence column becomes a set of filenames nobody can open |
| 68 | 10 | Final tally reported as **147 passing / 7 failing** browser checks, with the 7 named as the pre-existing baseline | The baseline was recorded before any Explore code was written (#5). Reporting "all green" would require either fixing a database this container does not have, or quietly dropping two suites | Nothing to change; this is a measurement |
| 69 | 11 | The sky is **ours**: a 40 km dome inside the scene, with a real sun disc and moving cloud, rather than MapLibre's | MapLibre's sky sits behind everything three.js draws, so the ground plane hides most of it; it has no sun in it, only a gradient the sun tints; and it stops existing when a basemap style fails to load, which is normal on a corporate network and the state a demo is most likely to be given | Delete `sky3d.ts` and go back to a gradient with no sun in it. The owner would also get back about 30 lines of shader |
| 70 | 11 | The sun disc is drawn at about **four degrees across**, roughly eight times life size | Half a degree is three pixels at this field of view — a dot nobody reads as the sun. Every architectural render enlarges it for the same reason, and four degrees is still far too small to compete with a Goldenrod band | Set `uSunSize` back to 0.0004 and the sun is technically present and practically invisible |
| 71 | 11 | Cloud is **white to pale grey and thin**, with only a hint of sun warmth, at a fixed amount per hour | A dramatic sky is the easiest thing to add and the fastest way to lose the one rule: orange-lit cloud at golden hour is large, saturated and moving, which beats a static band on every axis at once. Weather is a stated non-goal, so the amount is a constant, not a model | Raise `cloudFor` and the sky starts competing for attention. It is one number per hour |
| 72 | 11 | Cloud noise runs at **five lattice cells per unit** of the projected plane, not one | At the first frequency a camera near the zenith was inside a single noise cell magnified across the frame, so straight up had no cloud in it at all while the horizon looked correct. Found by pointing the new free camera at the sky — which is the reason the free camera earned its place | Lower it and overhead goes back to a flat wash |
| 73 | 11 | The ground plane is **120 km across** and fades to the horizon colour by 14 km | At 12 km its own edge was a visible hard line across the middle of every skyline frame | Shrink it and the edge comes back |
| 74 | 11 | The rivers are drawn in three.js with a **Fresnel sky reflection, moving ripples and sun glitter**, not as a flat blue fill | Water is the one surface in this scene that genuinely is not matte, and a flat polygon reads as a hole in the map. The flat map's deck.gl water cannot be reused: opaque water on deck.gl's canvas paints over the towers standing in front of it | Replace `water3d.ts` with a flat `MeshBasicMaterial` — about 60 lines lighter, and the river stops reading as water |
| 75 | 11 | Water **islands are dropped, not cut** | Holes need a triangulator with hole support this project does not have. At this scale a dropped island is a few square metres of river where there should be rock | Add hole support to `tessellate.ts` — a day, and it buys back a rock |
| 76 | 11 | A street is a **real section**: roadbed at +0.02 m, kerb face up to +0.16 m, pavement on top, and the footway is two ribbons either side rather than one wide one under everything | The old single ribbon put the pavement *below* the roadbed, so there was no kerb, so the street had no edge. Seen from a drone that is invisible; at 1.68 m it is the difference between a street and a car park | Revert to one ribbon: simpler geometry, and the street loses its edge again |
| 77 | 11 | Wide roads get a **broken lane line**, in the pavement's own tone rather than white | The art direction asks for no decals, and this is the one argued exception: a lane line is what makes an expanse of grey read as a road at eye level. Drawn in tone it is a texture at fifty metres and gone at two hundred, which is the opposite of how a decal behaves | Delete `laneDashes` — one function, and the carriageway goes back to plain grey |
| 78 | 11 | **The ground plane no longer writes depth** | It is a backdrop 120 km across sunk five centimetres below the streets, and it was inside one depth-buffer step of them at any distance. Nothing in the scene is below it, so it can never legitimately occlude anything | Give it `depthWrite: true` back and the streets start losing the depth test at distance |
| 79 | 11 | **Ground ribbons are wound anticlockwise seen from above** — the fix that made the streets appear at all | The strip normal is derived from the direction of travel, so reversing a road reverses the normal too and the winding comes out clockwise either way: every road in Manhattan was back-facing at once. It looked exactly like geometry that had never been built, which is where the time went. The cars were visible the whole time, driving down roads that were not there | None. This was a bug, not a decision — the row exists so the next person recognises the symptom |
| 80 | 11 | `useStreetscape` and `useCityContext` **clamp their bbox to what the endpoint will serve**, and judge a reply against the currently wanted bbox rather than a per-effect `cancelled` flag | Two independent faults with one symptom. The padding declined to widen an oversized viewport but sent it anyway, so a wide window got a 400; and the effect re-runs on every zoom change through the opening fly-to, so the reply that did arrive belonged to a superseded run, was cached, and was thrown away with nothing left to re-request it. Between them the streets, the water and the surrounding city often never arrived at all | None — both are bugs. They are in shared hooks, so the flat map gets the fix too, which is why the seven existing suites were re-run |
| 81 | 11 | Cars and people are built from **parts** — wheels, a windscreen rake, a head and two legs — at ~220 and ~90 triangles each | The old argument was that a car is never more than a few pixels wide. That is wrong about where the camera goes: walk mode puts the eye at 1.68 m, where a car is a metre away and a rectangular block reads as a crate. Still two draw calls, and ~320,000 triangles against a two-million budget | Revert to boxes: 300,000 triangles back, and the street-level view loses most of what makes it believable |
| 82 | 11 | Per-part shading rides in the **vertex-colour attribute** so tyres, glass and paint come out of one material | Three materials would cost three draw calls per thousand vehicles, which is the entire draw-call budget spent on traffic | None that keeps one draw call |
| 83 | 11 | Vehicles and pedestrians get a **hashed value variation, never a hue variation** | A street where every car is the same grey reads as a repeated asset. A grey at 0.8× and a grey at 1.25× both have zero chroma, so the hierarchy cannot be affected. Hashed rather than random so a car is the same grey between frames and reloads | Set the spread to zero: one line, and the traffic goes back to looking stamped |
| 84 | 11 | Pedestrians now **turn to face their direction of travel**; supersedes the earlier decision not to | That decision was right for featureless boxes and wrong once they have arms: leaving the rotation out points every pedestrian on the island at the same compass bearing, which reads as a formation. The cost is the occasional turn-on-the-spot at a junction | Drop the rotation and accept the formation |
| 85 | 11 | **Free look**: a camera of Explore's own, at any pitch including straight up, behind its own button | MapLibre's camera cannot look above the horizon — pitch is capped at 85 and 90 is level — so there is no value of any MapLibre parameter that puts the sky, the sun or the top of a tower in the middle of the frame. A model with a sky in it you are not allowed to look at is not a model | Delete `useFreeCam.ts` and `freecam.ts` and the mode is bounded by the map camera again. The screenshots that show the sun and the clouds become unreachable |
| 86 | 11 | Free look **switches deck.gl's layers off** for its duration, and picking with them | deck.gl projects with MapLibre's camera, which is no longer the camera the frame is drawn from, so a name-plate would sit over open sky several blocks from its tower. Off is strictly better than wrong. This is only acceptable because the bands are three.js geometry — the one rule survives free look intact | Leave them on and accept labels in the wrong place, or move deck.gl onto the free camera too, which is the interleaved-overlay work already priced at ~3 days in #65 |
| 87 | 11 | Free look **starts from exactly where the map camera already is**, and disables MapLibre's own drag, zoom and keyboard handlers while it runs | A camera mode that opens by throwing away the view you just framed is a mode people press once. And with MapLibre's handlers live, a drag pans a basemap nobody can see while the visible city stays put, which reads as the map having frozen | Let it start at a fixed vantage instead — one less inversion to maintain, and a jolt every time it is pressed |
| 88 | 11 | Free look's **pitch stops at 89.5°, not 90°** | At exactly vertical the forward vector is parallel to world up, `lookAt` cannot choose a roll, and the view snaps through a half turn. Half a degree short is indistinguishable and cannot degenerate | None — 90 is a singularity, not a preference |
| 89 | 11 | Free look **does not carry data loading with it**: the streetscape and the surrounding city still follow MapLibre's viewport | Those hooks are shared with the flat map and keyed to `map.getBounds()`. Re-keying them to a second camera touches the live map's fetch path, which is exactly the class of change this run does not make without being asked | Fly a long way in free look and you run off the edge of the loaded streets and context. Fixing it means giving both hooks a camera-agnostic bbox source — perhaps half a day, and it changes code the flat map depends on |
| 90 | 12 | The cloud layer **fades out before the projection stretches**, rather than being thickened there | The plane projection dir.xy/up grows without bound near level, so a few degrees above the horizon one noise cell was smeared across hundreds of pixels and the sky visibly pulled apart at the rim. The earlier "thicken it instead" trick made the streaks denser, not fewer. Fading the reach out is honest about the geometry | Raise the fade distance and the streaks come back; lower it and the cloud band stops short of the skyline |
| 91 | 12 | Free look **keeps the cursor** — the pointer is captured only while the button is held | Holding the pointer captured the whole time is right for a game and wrong for a tool: with no cursor there is nothing to point at a building with, and this is a mode of a map whose purpose is clicking on buildings. A press that does not travel five pixels is a click, not a look | Go back to always-captured: one condition, and free look stops being able to select anything |
| 92 | 12 | Free look does its **own picking**, by raycasting the massing three.js drew | deck.gl owns picking everywhere else and cannot answer here — it is projecting with MapLibre's camera and this frame was not drawn from it, so its answer is a building several blocks from the cursor. The raycast returns a building and an elevation; the caller turns the elevation into a floor with the same arithmetic the bands use | Leave free look unclickable. It was that until now, and it is the difference between a viewer and a tool |
| 93 | 12 | A click in free look that lands on a floor carrying an availability **goes inside it** | It is the shortest path from "that tower has something on 23" to standing in it looking out, and it is what the owner asked for in as many words. A floor with nothing on it selects the building instead, which is what a click on a building means everywhere else | Make it select-only and add a second step. One more click each time, for a mode whose whole value is immediacy |
| 94 | 12 | The building you are standing in is **not pickable** | Its glass is six inches from the camera, so every click would resolve to the space you are already in. Skipping it is what makes "click the tower opposite to move into it" work | None — this is what the feature requires |
| 95 | 12 | Inside a space the host building's facade is replaced by a **mullion grid at 6% glass**, not made double-sided | Double-siding leaves an opaque curtain wall between you and the view: the 23rd floor was a grey box. From a tenant's side a curtain wall is nearly all glass and what you see is the frame and the city through it. The grid runs on the same bay and storey pitch as the outside, so it lines up with the floor you are on | Hide the host entirely — simpler, and you stand on a slab in mid-air with no window |
| 96 | 12 | Entering a space seats the camera **two-thirds of the way toward the glass**, not in the middle of the plate | The walk starts in the middle on purpose — a nose against the window is disorienting. This mode exists to answer "what does it look like out of here", and from the dead centre of a plate the slab hides the street completely. Marched outward step by step so a notched or L-shaped plate needs no special case | Seat it centrally and the first thing anyone does is walk forward. Two lines |
| 97 | 12 | Space explore is **the free camera confined**, not a second camera | It reuses `holdInside` — the walk's own containment, the same ring and the same radius — so the two ways of being on a floor cannot disagree about where the glass is. And it is the only camera that can look up out of a window | Build a third camera. More code, and a new way for the glass to be in two places |
| 98 | 12 | Free look **carries its data loading with it**: the streetscape and the surrounding city load around the free camera | Previously they keyed off MapLibre's viewport, so flying two blocks ran off the edge of the loaded ground. Both hooks now take an optional focus point and free look reports its own position every 250 m | Revert the parameter and accept the edge. It is one argument, defaulted, and the flat map never passes it |
| 99 | 12 | The store gained a **mode machine with tests**: entering a space turns on Explore and free look and turns off the walk, leaving free look leaves the space, going flat clears all of it | Four states enterable from five places, and every failure is the same shape — two cameras driving `jumpTo` at once, or a state left set that nothing can clear. A broker stuck inside a floor plate is not a bug you find in a screenshot | None. These are the transitions the buttons already imply; the tests only write them down |
| 100 | 12 | `window.__app` exposes the store to the browser harnesses | `window.__explore` already exposes the scene for the same reason: the checks assert on state and pixels, and some of that state — which space, which floor — has no pixel of its own. It grants a harness nothing a user could not do with the buttons | Drive everything through clicks. Slower and more brittle, and some of it is not reachable at all while deck.gl is off |
| 101 | 12 | The development fixture can be regenerated **`--live --with-tenants`**: real availability, synthetic tenancy | The real database holds zero tenancy rows, which is true of the market data and left three `verify-occupancy` checks with nothing to act on. Tenancy is the only thing added; every availability, rent, landlord and provenance field stays exactly as the live API returned it, every invented row is stamped FIXTURE, and the file is gitignored | Leave those three checks failing on a data condition. The flag is opt-in and off by default |
| 102 | 12 | `verify-occupancy` now **reads the tenant it searches for out of the API** instead of hard-coding one | It searched the literal string "Kestrel", a company in the old synthetic fixture — so it was asserting about the fixture, not the search, and failed the moment the fixture became real data. Reading the name from the data also makes the check stronger: it now verifies the building the search lands on is the one that tenant is in | None. This was a weak assertion, found and tightened |
| 103 | 12 | `resolveAddress` answers the "do we already hold this?" half **from the fixture** when there is no database | Resolving is two questions and only the second needs a database, but the first was never reached without one — so the form told people a real building did not exist. The fixture already stands in for the database on the read path; this is the same substitution, and it is read-only. Creating still requires a real connection and still says so | Leave the five `verify-add-by-hand` checks failing on the container having no connection string |
| 104 | 12 | The walk harness holds `W` for five seconds rather than one and a half — **the distance bar is unchanged** | `stepWalk` clamps its timestep to 0.1 s per frame so a restored tab cannot teleport the walker. Under SwiftShader a frame now costs about 0.4 s, so a second of held key buys a quarter-second of movement. Giving the renderer enough frames is the honest fix; lowering the distance would not have been | None — the assertion is still "more than a metre and a half" |
| 105 | 13 | Mouse look is a **mode you enter**, not a button you hold: one click captures the pointer, a crosshair marks the centre, a click while captured selects what it is on | This has been all three ways and drag-to-look was the worst of them — it made you hold a button down for as long as you wanted to steer, which nobody will do while crossing Midtown. Always-captured has no cursor and this is a map. One click, hands free, is what every 3-D tool does | Go back to drag-to-look: one handler, and steering costs a held button again |
| 106 | 13 | Flying is **90 m/s, Shift ×8**; indoors is 1.9 m/s and Shift does nothing | 55 m/s made crossing the loaded market a chore. Indoors the same speed crosses a floor plate in under half a second, which makes a room impossible to look at | Two constants |
| 107 | 13 | A click on a floor **opens its card**; it does not teleport you inside | Going straight in was the first design and it is too much — you click a stripe to find out what it is, and being dropped on the 23rd floor before reading the rent is disorienting and hard to undo. The pointer is handed back at the same time, because a card you cannot reach with the mouse is not a card | Restore the teleport: one branch, and every exploratory click becomes a journey |
| 108 | 13 | The band is **not drawn on the building you are inside** | A collar is a stripe around the outside of the glass. From a desk it reads as somebody having painted the only view the space has — the band doing its job exactly where that is wrong. Hidden for the whole building, not the one floor, because the floors above and below cross the same glass | Show them and accept a Goldenrod stripe across the window |
| 109 | 13 | Every space gets a **default fit-out**: desks, chairs, screens on a 3.2 m module | An empty plate is honest and useless — a 4,000 SF floor and a 40,000 SF floor look identical because there is nothing in frame whose size anybody knows. A desk is 1.6 m and everyone has stood next to one. It is a ruler, not a floor plan, and the card says so. Three draw calls | Leave the plate bare and lose the sense of scale that is the mode's whole point |
| 110 | 13 | **Outside, this floor**: the plate stops being a wall and becomes an altitude | Answers what a floor plan cannot — what is on this side of the tower at this height, what does the building opposite look like from here, is the view about to be built out | Remove the toggle; the altitude lock is four lines |
| 111 | 13 | A card in the corner while inside, with **comparables you can click into**, and a Back that returns | It is what turns a tour of one space into a tour of a shortlist. Comparability is deliberately crude — nearby, similar size — and labelled as such: a scoring model here would look like a valuation and a broker's own shortlist beats anything this could invent | Drop the comps list and the mode tours one space at a time |
| 112 | 13 | **Reset the view moved to the top** and means three different things by mode | The bottom was right while the map was something you looked down at. At street level and inside a space the bottom of the frame is the floor you are walking toward and the top is sky. And "reset" means leave the room, or pull back where you are, or go home — a button that says one thing and does three is a button nobody trusts twice | Put it back at the bottom and make it always mean home |
| 113 | 13 | **The street-level walk button is removed.** The walk itself stays, reached from "Stand on floor N" | Free look does everything it did and the thing it could not: MapLibre's camera stops at 85 degrees, so the walk can face a facade but never look up it. Two buttons that both mean "put me on the ground", one of which cannot look at the sky, is a choice nobody should have to make | Restore the button. The hook and its harness section are untouched |
| 114 | 13 | Name-plates in free look are **DOM, projected through the layer's own matrix**, placed nearest-first, and a colliding pill is **dropped rather than nudged** | deck.gl cannot draw them from a camera it is not projecting with. Nudging is the obvious fix and is worse: a pill pushed forty pixels sideways points at the wrong tower, which in a city of towers is a wrong answer rather than an untidy one. Clicking one brings it forward permanently | Leave free look unlabelled, which is where it started |
| 115 | 13 | The flat map's labels get **`CollisionFilterExtension`**, priority = availabilities first then height | Overlapping pills were drawn on top of each other and neither could be read, which is worse than one being absent. The extension tests at the real drawn size in its own pass, so it is exact. `@deck.gl/extensions` was already installed as a transitive dependency; it is now explicit | Remove it and accept stacked pills. The priority rule is one expression |
| 116 | 13 | Labels are **pickable** now | They were unpickable on the grounds that a click should land on the massing. That was right while a pill could be sitting anywhere; once they cannot overlap, a name is an unambiguous target and a name you can read is a building you can open | One prop |
| 117 | 13 | **At night the whole city lights its windows**, context massing included, and the context interior is lit ~1.9× harder than the towers that carry data | The surrounding city has no fenestration by day — correct, it is scenery — so at night it started from black and read as a hole behind a lit Midtown. It starts from black rather than from a sunlit facade, which is why it needs more, not the same | Set the night glass fraction to zero and the context city goes dark again |
| 118 | 13 | **Street lamps after dark**: an emissive head on a pole, every 34 m on both kerbs of the wider roads | New York's roadway is legible after dark because it is lit, and the double row of points up an avenue is most of what the city looks like from a tower at 9pm. The head is warm — allowed because a lamp is two pixels across, so it has chroma and almost no area, the opposite of what threatens the hierarchy | Delete the lamp block; the night streets go back to unlit grey |
| 119 | 13 | Traffic roughly **doubled and sped up** — 900 cars at 6 m/s, 1,300 pedestrians | 420 cars was a car every couple of blocks, which read as an unusually quiet Sunday on the densest street network in the country. 3.2 m/s is the famous average *including* standing at lights, and this model has no lights, so every car crawled continuously | Four numbers |
| 120 | 13 | **Parks are grass and street trees are trees**, in a heavily desaturated sage | Explore mode drew every park as pavement and no street trees at all, on an island whose most famous feature is a park. Green is a hue and this product has one; the treatment is as far as it can be pushed without a large coloured area competing with a band | Lower the `greenFor` mix and the parks go back to reading as paving |
| 121 | 13 | Trees capped at **1,600** and lamps at **1,400**, down from 3,000 and 2,200 | The first figures put the frame past the harness's catastrophe ceiling — 472 ms against a 400 ms bound on SwiftShader — and the trees alone were a third of the scene. The ceiling was not raised to get green; the cost was taken out. An avenue lit every 34 m for a kilometre is already unmistakably a lit avenue | Raise the caps and the software-rendering check fails again. On a GPU neither figure is near a limit |
| 122 | 14 | Facades have an **archetype by era** — punched masonry, ribbon, curtain wall — not one grid with a different glass fraction | This is the single biggest reason a procedural city reads as fake. Every building having the same wall-to-window ratio makes every building the same building at a different height, and it is wrong about New York specifically, where a 1910 loft and a 1985 tower stand on the same block and look nothing alike | Set `uArchetype` to 2 everywhere and the city goes back to one material with holes in it |
| 123 | 14 | Materials vary by **warmth, not value** — every stone between about 0.78 and 0.94 | The first attempt separated them by value too, dropping brick to 0.62 and bronze to 0.44. At a low sun the whole city went dark and heavy and the near-white city was gone. Half the point of a pale city is that a Goldenrod band has somewhere quiet to be loud | Push the values apart again and watch the frame go grey. The hierarchy check at 0.35 chroma is the guard |
| 124 | 14 | Brick gets **coursing**, and masonry gets piers every third bay rather than every fifth | A brick wall at fifty metres is a very fine horizontal stripe, and its absence is why a punched-window building rendered as a grey slab with holes in it. Four courses to a storey rather than real coursing, which is sub-pixel and aliases into a shimmer | Two lines of shader |
| 125 | 14 | The fit-out is a **layout**, not a grid: perimeter offices, meeting rooms, open-plan benching, with glass partitions | One grid of identical desks told you the floor was big and nothing else. What a broker reads in a plate is how deep it is — how much can have a window and how much cannot — and a cellular perimeter against a benched core is exactly what makes that visible. Three desk types, a table, and partitions with a door gap | Revert to the single grid: simpler, and the plate stops saying anything about its own depth |
| 126 | 14 | Layout is decided by a **hash of the cell and the floor height** | The same floor is laid out identically on every visit and two floors of the same tower are laid out differently. Random would re-shuffle the room every time you walked back in | None — random here is strictly worse |
| 127 | 14 | Indoor movement raised from 1.9 to **4.2 m/s** | 1.9 was a walking pace and too literal: you are inspecting a room, not simulating a person, and crossing a large plate took twenty seconds of holding a key | One constant |
| 128 | 14 | The surrounding city is **tiered**: surveyed where the city has it, stepped-with-parapet above 92 m, extruded box below | Applying the full treatment to every footprint is unaffordable — and the cost that bit was not the frame budget but the *build*: nine hundred footprints of stepping and parapeting is several seconds of main-thread tessellation on viewport load. The harness caught it as the surrounding city simply never arriving. Thirty storeys is where a New York building stops being a box and starts being a silhouette | Lower the threshold and the page stalls when a viewport loads. Raise it and tall context goes back to flat-topped boxes |
| 129 | 14 | Free look draws **transit markers**, and they beat name-plates for the same pixels | deck.gl cannot draw them from a camera it is not projecting with, same as the labels. A broker showing a space is asked "how far to the subway" far more often than what the building next door is called | Drop them from the anchor list; it is one loop |
| 130 | 14 | The bbox caches are **bounded at twelve viewports** | They were unbounded and lived for the life of the page — fine when the map was open-look-close, not fine now that free look can fly across the borough and pull a payload every 250 m | Raise or remove the cap and accept the growth |
| 131 | 14 | The hour **re-tints** the streets and the planting; it only rebuilds when the lamps cross dusk | Rebuilding hundreds of thousands of triangles to change four material colours is a visible stall for nothing — the geometry is identical at every hour. The street lamps are the exception: they do not exist before dusk and cannot be tinted into existence | Go back to rebuilding: simpler, and every press of the clock stalls |
| 132 | 15 | Park fills use the **ear clipper's own winding, unchanged**, and the material is double-sided | This was emitted with the second and third indices swapped by analogy with the street ribbons, and it was exactly wrong. A ribbon's winding is derived from the direction of travel and comes out clockwise either way; a triangulated ring does not — `toCCW` guarantees the input and the clipper preserves it. Every park in Manhattan was back-facing. Central Park was present, correctly sized, visible, and drawing nothing | None — this was a bug. The row exists because it is the *second* winding assumption in this codebase to cost hours, which is why the material is now double-sided as well |
| 133 | 15 | The harbour is **one fixed box, fetched once, water only** | Every other ground layer is keyed to the viewport, which is right for streets. Water is the exception: it is what the horizon is made of, and clipped to the viewport the Hudson ends in a straight line a few hundred metres out and the island appears to float on the same grey plane as the streets. A fixed box is one edge-cached request that never changes shape, so the shoreline never moves or re-tessellates | Follow the camera instead: more requests, a shoreline that pops, and no benefit — Manhattan is not somewhere you can fly out of here |
| 134 | 15 | The far harbour uses a **cheap water shader** — no ripples, no glitter | Six noise lookups and a 90-power specular is the right cost for the river forty metres away and the wrong cost for twenty square kilometres of harbour: at two kilometres a ripple is far below a pixel. With the full shader the harbour cost about 150 ms a frame under software rendering and failed the catastrophe check, for an effect nobody can resolve. The Fresnel is kept, because it is cheap and it is what makes a surface read as water | Give it `detailed = true` and watch the frame check fail again |
| 135 | 15 | Boats: **two instanced meshes, deterministic placement, no traffic data** | A river with nothing on it is a river nobody is using, and New York's are among the busiest in the country. The wake is more visible than the hull at any distance you see them from. It claims "this water is in use", which is true, and nothing about any particular vessel | Delete `seedBoats`; the harbour goes still |
| 136 | 15 | The **Statue of Liberty is built, not downloaded** — about forty triangles of box and cone at her real coordinates, with an emissive torch after dark | A downloaded mesh is a licence question, a load-order problem and a megabyte, for a shape that is three hundred pixels tall at most from two miles away. The outline is the only part that has to be right, and everybody already knows it. The torch is the one thing that brightens at night, because that is what you can actually see from the Battery | Swap in a real model and take on the licence and the load |
| 137 | 15 | She is **weathered copper**, the one deliberate hue in the model | It is unavoidable and it is fine: a single object a hundred pixels tall from anywhere in Manhattan cannot threaten a band, and desaturating her into a grey statue would be its own kind of wrong | Grey her out; one colour |
| 138 | 15 | Space explore gains a **search box** | The comparables answer "what else is like this near here", which is the question you ask once you are standing somewhere. Search answers "take me to the one I was told about" — the only way to reach a space that is neither nearby nor a similar size | Remove it and rely on the filter rail, which means leaving the mode to use it |
| 139 | 15 | Floor plates are **cached between visits**, bounded at twenty and cleared when the surveyed asset lands | Touring six comparables re-derived six plates. A plate is a pure function of a building and a floor so the cache cannot be wrong — except across the surveyed asset arriving, which changes the cross-section it is cut from, hence the clear | Drop the cache: a few milliseconds per space, and one less thing to invalidate |
| 140 | 15 | Free look's data refetch is throttled by **time as well as distance** | Distance alone was the whole rule and it is not enough: with Shift held the camera covers the threshold in a third of a second, so a sprint across Midtown queued a new streetscape three times a second and every reply is a full rebuild | Remove the interval and accept the thrash |
| 141 | 15 | The surveyed massing is kept in **Cache Storage with an ETag check**, not left to the HTTP cache | `force-cache` asks the HTTP cache, which is allowed to evict whenever it likes — so the 150 KB asset that decides whether the whole city has setbacks is re-downloaded on a schedule nobody controls. Cache Storage survives reloads and is not evicted casually, and this codebase already uses it for Google's tiles. The conditional request is what stops a stale copy outliving a regenerated asset | Revert to `force-cache`: one line, and the asset is re-fetched at the browser's discretion |
| 142 | 15 | **No service worker.** Persistence is done with `caches` directly, for one asset | A service worker intercepts navigation and every request on the origin, which is a large new failure surface for a tool brokers use in front of tenants. The benefit here is one 150 KB static file; the direct Cache Storage call gets all of it and can only affect the one URL it names | Ship a service worker and take on the scope. It is the owner's call, not mine — this is the "change brokers would see" the plan reserves |
| 143 | 16 | The surrounding city becomes **3-D Tiles, built offline** — a quadtree of `.glb` nodes with geometric errors, not runtime extrusion | Extruding footprints in the browser capped out at 220 stepped buildings because anything more was seconds of main-thread tessellation on every viewport change, and it was still only footprints — no real roofs, no setbacks the survey knows about. Tiling moves all of it to build time, once, and the renderer then loads only what would otherwise be visibly wrong on screen. It is the same format and the same reasoning as Cesium's and Esri's New York demos, from the same source data we already ingest | Stay on runtime extrusion: a smaller repo, a simpler pipeline, and a city that is 220 boxes with flat tops |
| 144 | 16 | `3d-tiles-renderer`, **not** deck.gl's `Tile3DLayer` — even though deck.gl is already a dependency | Purely the compositing law this whole mode is built around: deck.gl's overlay is a separate canvas above MapLibre's, so a tiled city drawn there paints over the towers, the bands and the streets that three.js draws. `3d-tiles-renderer` renders into *our* scene, our depth buffer, our camera. Nothing else about the two libraries entered into it | Use `Tile3DLayer` and lose the bands behind the scenery — which is the one thing that must never happen |
| 145 | 16 | Tall buildings are **held at coarse levels** rather than pushed to the leaves (`keepAtDepth = [180, 90, 45, 22, 11, 0, 0]`) | A quadtree that partitions purely by position puts the Empire State Building in a leaf, and a leaf only loads when you are close to it — so the skyline would dissolve as you pulled back, and it would read as a level-of-detail setting rather than a partitioning bug. Height decides depth; position decides which child | Partition by position alone: correct, fast, and the skyline disappears at range |
| 146 | 16 | The tileset carries **no colour, no era, no fenestration** — a white unlit material the renderer replaces on load | What a building looks like is Explore's facade shader, and a second opinion baked into the asset is how the two drift apart under a new hour or a new preset. The shader's `along`/`up`/`wall` attributes are filled with defaults at load instead of shipped, because carrying them would roughly triple a tileset for geometry that is scenery | Bake appearance into the `.glb`: a bigger asset that stops responding to the hour |
| 147 | 16 | A **`frame.json` anchor guard** refuses a tileset built at a different origin, and falls back to extruded footprints | Every vertex is metres from one lon/lat. A tileset loaded at the wrong anchor is Manhattan a few hundred metres into the Hudson, consistently and with nothing about the frame looking wrong. This caught its own first real failure: the build script carried a default centre 250 m from `MapView`'s, and the guard refused the city rather than misplacing it | Drop the check and trust the operator to rebuild — the failure it prevents is silent and looks like a projection bug |
| 148 | 16 | A **hand-written glTF writer** (`src/lib/explore/glb.ts`), about 200 lines, no dependency | `THREE.GLTFExporter` needs a DOM, and every alternative is a large dependency for one node script writing one mesh layout. What we emit is two accessors and an index — the narrowest possible use of the format. The spec's sharp edges (4-byte chunk padding, spaces not zeros in the JSON chunk, required `min`/`max` on POSITION) are each asserted in `tests/explore-tiling.test.ts`, because a malformed GLB loads in some viewers and not others | Add an exporter dependency and a headless DOM to the build |
| 149 | 16 | Indoors, **Shift now works**, and the base speed rises to 5.4 m/s with a 2.5× sprint | Indoor movement was deliberately slower than outdoors and deliberately refused Shift, on the reasoning that a room is small. In practice a full floor plate is 60 m across and crossing it at a walk is most of what the mode asks you to do. Sprint indoors is a higher multiplier than outdoors because the distances are shorter and the ramp has less time to matter | Restore the walk-only indoor camera |

| 150 | 16 | The tiled city's bay rhythm is **derived in the shader** from world position and wall normal, not shipped as vertex attributes | A tile carries positions, normals and indices; the four attributes the facade shader wants would roughly triple a tileset for buildings that are scenery. Without them `vIsWall` is zero, the whole bay treatment is skipped, and every surveyed building in Manhattan renders as flat paint. The derived version starts its bays at an arbitrary phase on a wall that does not pass near the frame origin — and on a building nobody stands inside, a correct rhythm in the wrong phase is indistinguishable from the right one, while a grey slab is not | Ship the attributes and take the size, or accept a flat-painted city |
| 151 | 16 | `TilesRenderer` gets **a camera of its own**, never the scene's | This layer puts a complete world→clip matrix into `camera.projectionMatrix` and leaves the camera's world matrix identity. Setting `camera.position` on it to tell the tile renderer where the eye is put a view transform on top of a projection that already had one, and drew the entire city translated by the eye position — which reads on screen as the tileset painting white over everything, not as a camera fault. The replacement is exact rather than approximate: world matrix `T`, projection `P·T`, so `P·T · T⁻¹ = P` | None. This was a bug, and the row exists because the symptom pointed at shading, winding, occlusion and scale, and was none of them |
| 152 | 16 | Error target **24 px**, cache 180/420 tiles — raised and shrunk after the first measurement | Twelve pixels refines the quadtree several levels deeper than anything visible warrants and the cost is paid every frame. Each level is four times the last, so doubling the target roughly quarters the resident tiles. **Not yet re-measured against the frame budget**, which is the honest state of it | Put it back to 12 for detail that cannot be resolved, and pay for it on every frame |
| 153 | 16 | The orbit **hides the city by default**, with a button to bring it back | A tower crossing in front of the subject on every revolution is exactly what ruins a shot of one building, and the mode exists to produce that shot. Making it a default the user can undo rather than a fixed behaviour is the difference between a camera move and a diorama | Leave the city in and let it cross the frame |
| 154 | 16 | The keyboard is **dead** while the orbit runs, and stopping is a hand-off rather than a transition | An orbit you can nudge with W is a camera with a drift, not a locked shot, and the whole value is that the framing does not wander. Stopping clears the orbit and nothing else: the free camera is already standing at the last orbit position, so it resumes from exactly there — which is what "return me to free look from where it is standing" has to mean | Blend the keys in and lose the lock; or snap somewhere on stop, and lose the place |
| 155 | 16 | The orbit is **suspended, not cleared**, while a space is explored | Going inside and coming back out is a round trip, and a round trip that ends somewhere other than where it started is a navigation problem the user then has to solve. Suspending keeps the subject, the framing and the angle, so stepping outside resumes the same shot | Clear it, and drop the user into free look next to a building they now have to find again |
| 156 | 16 | Corner cells are **management offices**, chosen by measured distance from the plate's centre rather than by a hash | Every floor plate in the city works this way and nobody chose the convention: a corner has two walls of glass and twice the light. Measuring it rather than hashing it means the tier lands where a plan would put it on any shape of floor, including the setback and L-shaped plates that have corners where a rectangle does not | Hash them like the other tiers, and watch corner offices appear in the middle of the floor |

---

## Superseded

Move nothing here. Add a new row above that says "supersedes #n", and note the
number here with a one-line reason, so the trail stays readable.

| # | Superseded by | Why |
|---|---|---|
| The no-rotation half of #— (pedestrians, sprint 8) | #84 | Correct for boxes, wrong for figures with arms |
| The horizon-thickening half of #71 | #90 | It made the rim streaks denser rather than removing them |
| The always-captured pointer in #85 | #91 | A mode of a map has to be able to click on the map |
| The drag-to-look compromise in #91 | #105 | Holding a button to steer is not something anyone does for long |
| "A click goes inside the space" (#93) | #107 | Too much for one click; it opens the card instead |
| The walk button (sprint 6) | #113 | Free look supersedes it and can also look up |
| "Four stone values inside six percent" (sprint 4) | #122, #123 | It is why the city read as one material |
| The single-grid fit-out (#109) | #125 | A grid says the floor is big and nothing else |
| "The hour rebuilds the streets" (#131's predecessor) | #131 | The geometry does not change with the hour |
| "Free look does not carry data loading with it" (#89) | #98 | Closed — both hooks now take a focus point |
| "Picking is off during free look" (#86) | #92 | Closed — free look raycasts the three.js massing instead |
